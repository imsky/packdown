# Document with space encoding

## hello.js

```js
    if (true) {
      alert('hello');
    } else {
      alert('world');
    }
```

## world.md

```md
    # A Markdown Document

    Here's a Markdown document within a Packdown document, with its own code blocks:

    ```js
    hello.world();
    ```
```